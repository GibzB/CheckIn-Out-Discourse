#to be coded using REST API


const JSX = (
  <div>

  {/* here lies the code implementation*/}
    <h1>This is a block of JSX</h1>
    <p>Here's a subtitle</p>
  </div>
  
  {/*added a class below*/}
  <div className="myDiv">
    <h1>Add a class to this div</h1>
  </div>
  
  
);

class TypesOfFood extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <div>
        <h1>Types of Food:</h1>
        {/* change code below this line */}
        <Fruits />
        <Vegetables />
        {/* code above this line is a COMPONENT and has been rendered/nested to the TypeOfFood class */}
      </div>
    );
  }
};

{/* here lies the DOM syntax*/}
ReactDOM.render(<TypesOfFood />, document.getElementById('challenge-node'))
